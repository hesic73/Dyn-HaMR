type: video
split: custom
# NOTE (hsc): 这里我在dyn-hamr下面创建一个软链接，指向vieo_root
root: null  # put your videos in root/videos/vid.mp4
video_dir: videos
seq: demo1
ext: mp4
src_path: ${data.root}/${data.video_dir}/${data.seq}.${data.ext}
use_cams: True
track_ids: "all"
shot_idx: 0
start_idx: 0
end_idx: 500
split_cameras: True
name: ${data.seq}-${data.track_ids}-shot-${data.shot_idx}-${data.start_idx}-${data.end_idx}
sources:
  images: ${data.root}/images/${data.seq}
  cameras: ${data.root}/dynhamr/cameras/${data.seq}/shot-${data.shot_idx}
  intrins: "/home/sichenghe/25fall/Dyn-HaMR/dyn-hamr/_video_root/videos/demo1_intrinsics.txt"
  tracks: ${data.root}/dynhamr/track_preds/${data.seq}
  shots: ${data.root}/dynhamr/shot_idcs/${data.seq}.json
